# Create a Publisher that publishes dummies values
# with a specific topic name and message type.
# The message type will be Objects from the zed_interfaces package.
# Make sure to install the zed_interfaces package correctly before running this code.
#
import rospy as rp
from zed_interfaces.msg import Objects



